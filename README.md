# Formify Website
Landing page for Formify

## Development
After cloning the repository:
- `npm install` to install dependencies
- `npm run dev` to test on localhost
- `npm run build` to create production site
